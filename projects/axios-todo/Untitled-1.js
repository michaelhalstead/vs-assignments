// Local Storage
    // Maintains data fter page close / refresh

// Session Storage
    // Deletes data on page close

// Save to Local Storage

localStorage.setItem('key', 'value')
localStorage.key1 = 'value1'

console.log(localStorage.getItem('key'))